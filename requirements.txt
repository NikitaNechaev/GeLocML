# Основные зависимости для GeLocML
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
torch>=1.9.0
torchvision>=0.10.0
biopython>=1.79
joblib>=1.1.0
tqdm>=4.62.0
click>=8.0.0
pyyaml>=6.0
psutil>=5.8.0

# Опциональные зависимости для улучшения производительности
# Установите при необходимости для ускорения вычислений
# cupy-cuda11x>=10.0.0; sys_platform != "darwin" and sys_platform != "win32"

# Зависимости для разработки (устанавливаются через pip install -e .[dev])
# pytest>=6.0
# pytest-cov>=2.0
# black>=21.0
# flake8>=3.9
# mypy>=0.910
# pre-commit>=2.15

# Зависимости для документации (устанавливаются через pip install -e .[docs])
# sphinx>=4.0
# sphinx-rtd-theme>=0.5
# myst-parser>=0.15